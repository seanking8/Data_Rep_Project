import React, { Component } from 'react';

// Component to display a heading
// Will be displayed when Header is seleted in nav bar
class Header extends Component {

    render() {
        return (
            <div>
                <h1>This is Header Component</h1>
            </div>
        );
    }
}

export default Header;