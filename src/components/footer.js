import React, { Component } from 'react';

// Component to display a heading
// Will be displayed when Footer is seleted in nav bar
class Footer extends Component {

    render() {
        return (
            <div>
                <h1>This is Footer Component</h1>
            </div>
        );
    }
}

export default Footer;